'''Checkio.org задача про робота пташку'''

'''Розшифрування по ключу:
після приголосної йде 1 зайва голосна зі списку VOWELS
після голосної йде 2 зайвих таких самих голосних
Наприклад:
translate("hieeelalaooo") --> hello
translate("aaa bo cy da eee fe") --> a b c d e f'''

VOWELS = "aeiouy"


def translate(phrase):
    # перетворюємо на список, щоб мати можливість змінювати
    new = list(phrase)
# заводимо лічильник проходження ітерацій для індексації елементів на видалення
    count = -1
# проходимося по списку вхідних елементів
    for x in new:
        '''для того, щоб лічильник відповідав індексу елемента який перевірятиметься
          ми почали з -1 і тепер додаємо по 1 з кожним циклом'''
        count += 1
        # перевіряємо чи є такий елемент в списку голосних літер
        if x in VOWELS:
            # якщо літера голосна то повертається True
            # і ми видаляємо два наступних елемента
            del new[count+1:count+3]
        # якщо елемент є пробілом то переходимо на новий цикл
        # із-за continue ми лічильник перенесли на початок циклу
        elif x == ' ':
            # переходимо до наступного елемента
            continue
        # якщо не голосна і не пробіл, то відповідно приголосна
        else:
            # і ми видаляємо наступний елемент
            del new[count+1]
    # складаємо все докупи в строку і повертаємо
    return "".join(new)
